{
  "name": "aws-k8s-argocd",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy": "pulumi up",
    "destroy": "pulumi destroy"
  },
  "dependencies": {
    "@pulumi/aws": "^5.0.0",
    "@pulumi/awsx": "^1.0.0",
    "@pulumi/eks": "^1.0.0",
    "@pulumi/kubernetes": "^3.0.0"
  },
  "dependencies": {
    "typescript": "^4.0.0"
  }
}
